{
  "name": "marching-squares-world",
  "author": "Andreas Schoch",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pages-predeploy": "npm run build",
    "pages-deploy": "tsc && vite build && gh-pages -d ./dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.0",
    "vite-plugin-singlefile": "^0.13.5"
  },
  "dependencies": {
    "simplex-noise": "^4.0.1"
  }
}
